# Build context is the repository root, because the Dockerfile needs the
# workspace manifests — hence this file lives here, not in apps/api.

node_modules
**/node_modules

dist
**/dist
build
coverage

.env
**/.env

.git
.github
.husky
.claude
.vscode
.idea

docs
README.md

# Prisma client: generated inside the image by the build step.
apps/api/src/generated

# The web source stays out of the API image, but its package.json does not:
# the root lockfile describes both workspaces and `npm ci` needs them present.
apps/web
!apps/web/package.json
